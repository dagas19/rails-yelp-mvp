<div class="container">
  <h1 class="text-center">Our Restaurants</h1>
    <% @restaurants.each do |restaurant| %>
        <%= link_to "See the details", restaurant_path(restaurant)%>
          <h2><%= restaurant.name %></h2>
          <p><%= restaurant.address %></p>
    <% end %>
</div>
